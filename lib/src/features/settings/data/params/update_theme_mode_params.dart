import 'package:flutter/material.dart';

class UpdateThemeModeParams {
  const UpdateThemeModeParams({required this.userId, required this.themeMode});

  final String userId;
  final ThemeMode themeMode;
}
